name: env-demo
on: workflow_call
env:
  APP_ENV: "dev"
jobs:
  env-demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: connecting to Env
        run: echo "App is connecting to $APP_ENV" 
